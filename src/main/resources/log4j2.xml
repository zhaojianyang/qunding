<?xml version="1.0" encoding="UTF-8"?>
<!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出-->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数-->
<configuration status="WARN" monitorInterval="30">
    <!--先定义所有的appender-->
    <appenders>
        <!--输出控制台的配置-->
        <!--<console name="Console" target="SYSTEM_OUT">-->
            <!--<ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>-->
            <!--&lt;!&ndash;输出日志的格式&ndash;&gt;-->
            <!--<PatternLayout pattern="[%d{HH:mm:ss:SSS}] [%t] [%p] - %l - %m%n"/>-->
        <!--</console>-->
        <RollingFile name="RollingFileError" fileName="${sys:user.home}/logs/qunding/error.log"
                     filePattern="${sys:user.home}/logs/qunding/error_%d{yyyy-MM-ddHH_mm}.log">
            <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="%m%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy modulate="true" interval="1"/>
            </Policies>
            <DefaultRolloverStrategy max="60">
                <Delete basePath="${sys:user.home}/logs/qunding" maxDepth="1">
                    <IfFileName glob="error_*.log" />
                    <IfLastModified age="60M" />
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>

    </appenders>

    <!--然后定义logger，只有定义了logger并引入的appender，appender才会生效-->
    <loggers>
        <!--过滤掉spring和mybatis的一些无用的DEBUG信息-->
        <logger name="org.springframework" level="ERROR"></logger>
        <logger name="org.mybatis" level="ERROR"></logger>
        <logger name="org.eclipse.jetty" level="ERROR"></logger>
        <root level="All">
            <appender-ref ref="RollingFileError"/>
        </root>

    </loggers>
</configuration>